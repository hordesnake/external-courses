    'use strict';
    let newStr = "     Max is a good boy     ";

    function ReturnStrWithoutSideSpaces(str) {
        newStr.trim();
        return newStr;
    };

    module.exports = ReturnStrWithoutSideSpaces;